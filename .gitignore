__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Answer key — distributed separately via Box, must NOT be committed (see plan §6.1)
data/refsol.py
**/refsol.py

# Large / generated. Sweep artifacts only -- small gate reports live in results/,
# which IS committed: they are the evidence behind a gate decision.
runs/
data/*.safetensors
*.log

# Local scratch
vendor/
*.bak
